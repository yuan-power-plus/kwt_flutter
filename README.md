# 科文通 (KWT Flutter)

一个用于查询科文通教务系统信息的 Flutter 应用，支持查看课表、成绩、等级考试等信息。

## ✨ 功能特性

- 📚 **个人课表查询** - 查看个人课程安排
- 🏫 **班级课表查询** - 查询指定班级的课程安排
- 📊 **成绩查询** - 查看课程成绩和 GPA
- 🎯 **等级考试查询** - 查看等级考试成绩
- 🔐 **用户登录** - 支持学号密码登录
- 📱 **响应式设计** - 适配不同屏幕尺寸
- 🌙 **现代化 UI** - Material Design 3 风格

## 🚀 快速开始

### 环境要求

- Flutter SDK: ^3.8.1
- Dart SDK: ^3.8.1
- Android: API 21+ (Android 5.0+)
- iOS: 12.0+

### 安装步骤

1. **克隆项目**
   ```bash
   git clone <repository-url>
   cd kwt_flutter
   ```

2. **安装依赖**
   ```bash
   flutter pub get
   ```

3. **运行应用**
   ```bash
   flutter run
   ```

### 构建发布版本

**Android APK:**
```bash
flutter build apk --release
```

**Android App Bundle:**
```bash
flutter build appbundle --release
```

**iOS:**
```bash
flutter build ios --release
```

## 🏗️ 项目结构

```
lib/
├── assets/           # 静态资源
├── common/           # 通用组件
├── constants/        # 常量定义
├── models/           # 数据模型
├── pages/            # 页面组件
├── services/         # 服务层
├── theme/            # 主题配置
├── utils/            # 工具类
└── main.dart         # 应用入口
```

## 🔧 配置说明

### 服务器配置

在 `lib/services/config.dart` 中配置服务器地址：

```dart
class AppConfig {
  static const String defaultServerUrl = 'http://your-server-url/jsxsd';
}
```

### 网络配置

```dart
static const Duration connectionTimeout = Duration(seconds: 30);
static const Duration receiveTimeout = Duration(seconds: 30);
```

## 📱 使用说明

1. **启动应用** - 应用启动后直接进入课表页面
2. **查看课表** - 在首页可以查看个人课表
3. **功能页面** - 点击底部"功能"标签访问其他功能
4. **用户登录** - 在"我的"页面或功能页面进行登录
5. **查询数据** - 登录后可以查询班级课表、成绩等信息

## 🛠️ 技术栈

- **前端框架**: Flutter
- **状态管理**: 原生 StatefulWidget
- **网络请求**: Dio + Cookie 管理
- **数据解析**: HTML 解析
- **本地存储**: SharedPreferences
- **UI 组件**: Material Design 3

## 📋 依赖包

- `dio`: HTTP 客户端
- `cookie_jar`: Cookie 管理
- `html`: HTML 解析
- `shared_preferences`: 本地存储
- `path_provider`: 文件路径管理
- `url_launcher`: URL 启动器

## 🔍 故障排除

### 常见问题

1. **网络连接失败**
   - 检查网络设置
   - 确认服务器地址正确
   - 检查防火墙设置

2. **登录失败**
   - 确认学号和密码正确
   - 检查验证码输入
   - 确认服务器状态

3. **构建失败**
   - 检查 Flutter 版本
   - 确认依赖包版本兼容
   - 清理构建缓存：`flutter clean`

### 调试模式

```bash
flutter run --debug
```

## 📄 许可证

本项目仅供学习和研究使用，请勿用于商业用途。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

- 作者：刘先森
- 微信：Sept_O-O
- GitHub：https://github.com/yuan-power-plus
- 邮箱：lly_6120@163.com

---

**注意**: 使用本应用时请遵守相关法律法规和学校规定。
